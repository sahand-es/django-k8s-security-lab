apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# Note: requires the escape-zone namespace and the `ctf-worker2` kind node
# (which mounts /var/run/docker.sock). F11 flag lives at ~/flag.txt on the VM
# host and is placed by the deploy script (not in-cluster).
resources:
  - legacy-worker.yaml
